# Browser Specific Notes

---

<b>NOTES :</b>
  - This page needs some love your input (pull request / discussion) is apprecated.
  - SPB makes selecting or switching brtween prfered browser stright forward.
  - This page aims to provide information about using browsers which SPB supports.

---

## Brave 

---
  - internal URLs
    - <b>```brave://components``` (update Brave profile connected components)</b>
    - ```about://discards``` (view / manually unload processes)
    - ```brave://flags ``` (configure brave options - eg disable auto extension updates and more...)
  - command line switches
    - --proxy-server="http://127.0.0.1:8080" (configures the proxy for the session)
    - --disable-background-networking
      - prevents subsystems typically perform activities like automatic connections to services, online certificate updates, network predictions, and analytics when you are not actively browsing
  - keyboard shortcuts
     - shift-esc (show browser task manager)
  - useful external links
    - [brave opt out](https://github.com/johnthagen/brave-opt-out) (currated list of various options to opt out of Brave Reqaerds, Ads and AI)
    - [brave debloatinator](https://github.com/MulesGaming/brave-debloatinator) (setup system wide policies for brave) 
---

## FireFox

---
  - internal URLs
    - ```about:processes``` (view FireFox processes)</b>
    - ```about:unloads``` (view / manually unload processes)</b>
  - enviroment variables
    - typically FireFox respects [proxy enviroment variabels](https://github.com/henri/spb/#proxy-settings) (configures the proxy for the sessions)
      - use caution, this is not always the case make sure you test your setup
---

## Additional Resources

This section contains additional resources for Chromium / Blink based browsers such as Brave.

  - [peter' blog - list of chromium command line switches](https://peter.sh/experiments/chromium-command-line-switches/) (not all of these are compatible with brave)
  - [chromium poject - official documentation on command-line switches](https://www.chromium.org/developers/how-tos/run-chromium-with-flags/)
  - [browswers cheat sheet gist](https://gist.github.com/henri/a454bb27edb3d3a567c5a695f0582aa7)

### Example of combining flags to use with chromium

```
# simply add and remove falgs as required
#
# if you are using spb with a single browser you may simply
# add flags to your spb fish alias to have these flags
# applied for all spb sessions started via your spb alias
#
# keep in mind that not all chromium dirvied browsers will respect all flags
# also, these flags often change over time. the list below may be outdated.

chromium-browser --disable-background-networking --disable-breakpad --disable-cloud-import --disable-databases --disable-preconnect --disable-speech-api --disable-sync --disable-voice-input --disable-webgl --incognito --media-cache-dir=/dev/null --disk-cache-dir=/dev/null --no-pings --no-wifi --disable-local-storage 
```

<br>

### potentially useful flags with explanation

<b>--disable-background-networking</b>

this switch disables several subsystems which run network requests in the background helpful when carryiong out network performance testing to avoid noise in the measurements. (may interfere with normal browser function)

<b>--disable-component-extensions-with-background-pages</b> 

disables default component extensions with background pages useful for performance tests where these pages may interfere with performance results (may interfere with normal browser function)

<b>--dns-prefetch-disable</b>

disables prefetching of DNS information and may reduce unwanted network traffic (potential for performance issues)
